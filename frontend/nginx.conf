server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Vue Router 使用 history 模式时，
    # /rating/1、/score/1 等前端路由直接访问或刷新，
    # 都需要回退到 index.html 交给 Vue Router 处理。
    location / {
        try_files $uri $uri/ /index.html;
    }
}